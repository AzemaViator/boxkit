[devbox]
image=ghcr.io/azemaviator/devbox:latest

home="${HOME}/projects/.devbox"

nvidia=true
root=false
pull=true
entry=false

init=true

volume=/var/lib/flatpak/:/var/lib/flatpak/:ro
volume="${HOMEBREW_PREFIX}":"${HOMEBREW_PREFIX}":ro
volume=devbox-tool-data:/var/lib/tool-overlay
